{
  "name": "openclaw-infra",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --require-approval broadening",
    "destroy": "cdk destroy --all",
    "test": "vitest --run"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.170.0",
    "constructs": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^4.7.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.4"
  }
}
